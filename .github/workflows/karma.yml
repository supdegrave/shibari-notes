name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         node-version: [12.x]

    steps:
    - uses: actions/checkout@v2
      
#     - name: Node $
#       uses: actions/setup-node@v1
#       with:
#         node-version: $

    - name: Run Client Karma tests
      run: |
        npm ci
        npm run test:client:headless
